<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.tigerapp.sparepart.MaterialListActivity">

    <TextView android:text="@string/materialListViewTitle" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/materialListViewTitle" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/materialListView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentLeft="false"
        android:layout_alignParentStart="false"
        android:layout_below="@+id/materialListViewTitle" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Search"
        android:id="@+id/searchButton"
        android:layout_above="@+id/materialListView"
        android:layout_toRightOf="@+id/materialListViewTitle"
        android:layout_toEndOf="@+id/materialListViewTitle"
        android:layout_marginLeft="39dp"
        android:layout_marginStart="39dp" />

</RelativeLayout>
